syntax = "proto3";

import "google/protobuf/any.proto";

package hub;

option go_package = "hub";

service Hub {
   rpc SelectServer (MessageJSON) returns (MessageJSON);
   rpc RunRoute (MessageJSON) returns (MessageJSON);
}

message MessageJSON {
   bytes body = 1;
}


